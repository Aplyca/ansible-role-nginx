 # {{ ansible_managed }}

server {
  {% for listen in item.listen %}
    listen {{ listen }};
  {% endfor %}

  {% for directive, value in item.directives|dictsort(true) %}
    {{ directive }} {{ value }};
  {% endfor %}
}
